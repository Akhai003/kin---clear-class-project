# V8.2 Safe patch

This package starts from the user's re-uploaded original ZIP.

Only these intentional changes were made:
1. The Shop mega-menu Featured / Newborn Starter Set visual now uses a curated real skincare-set photograph with a second remote fallback. The card layout and text were not changed.
2. A single animated transactional offer rail was added immediately above the existing footer. No header announcement bar was added, no product prices/cards were modified, and no page section was replaced.
3. Reduced-motion users receive a static offer rail.

Everything else is preserved from the uploaded ZIP.
